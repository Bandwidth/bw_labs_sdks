[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bw-stt"
version = "0.2.0"
description = "Python SDK for the Bandwidth Labs speech-to-text API"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Bandwidth"}]
requires-python = ">=3.10"
dependencies = ["websockets>=13"]
keywords = ["speech-to-text", "stt", "transcription", "streaming", "bandwidth"]
classifiers = [
    "Development Status :: 4 - Beta",
    "License :: OSI Approved :: MIT License",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
    "Typing :: Typed",
]

[project.urls]
Homepage = "https://github.com/Bandwidth/bw_labs_sdks"
Repository = "https://github.com/Bandwidth/bw_labs_sdks"
Documentation = "https://labs.bandwidth.com/docs/speech-to-text"

[tool.hatch.build.targets.wheel]
packages = ["src/bw_stt"]

[tool.ruff]
target-version = "py310"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.10"
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "example: runs the packaged example CLI against the in-process mock server",
]
